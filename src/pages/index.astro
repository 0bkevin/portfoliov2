---
import Layout from "../layouts/Layout.astro";
import Home from "../components/home/Home.astro";
import About from "../components/about/About.astro";
import ExperienceSection from "../components/experience/ExperienceSection.astro";
import ScrollUp from "../components/ScrollUp.astro";
import Contact from "../components/contact/Contact.astro";
import ProjectsSection from "../components/projects/section/ProjectsSection.astro";
import BlogSection from "../components/blog/section/BlogSection.astro";
import AchievementsSection from "../components/achievements/AchievementsSection.astro";
import { createPersonSchema, createWebsiteSchema, seoConfig } from "@lib/seo";

const title = "Kevin Bravo | Product Engineer, Full-Stack Software & AI Engineer";
const description =
  "Explore Kevin Bravo's software engineering portfolio, case studies, and technical writing on full-stack product engineering, internal platforms, AI agents, and backend systems.";
const image = seoConfig.defaultOgImage;

const webPageSchema = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  "@id": `${seoConfig.siteUrl}/#webpage`,
  name: title,
  description,
  url: Astro.url.toString(),
  isPartOf: {
    "@type": "WebSite",
    "@id": `${seoConfig.siteUrl}/#website`,
    name: seoConfig.siteName,
    url: seoConfig.siteUrl,
  },
  about: {
    "@type": "Person",
    "@id": `${seoConfig.siteUrl}/#person`,
    name: seoConfig.author.name,
    url: seoConfig.author.url,
    jobTitle: seoConfig.author.jobTitle,
  },
  primaryImageOfPage: {
    "@type": "ImageObject",
    url: new URL(image, Astro.site).toString(),
  },
};

const websiteSchema = createWebsiteSchema({
  name: seoConfig.siteName,
  url: seoConfig.siteUrl,
  description,
});

const personSchema = createPersonSchema({
  description:
    "Product engineer focused on full-stack products, internal platforms, AI agents, and backend systems, with full ownership from architecture to production.",
  worksFor: [
    {
      "@type": "Organization",
      name: "Proyecto Educa",
    },
  ],
  knowsAbout: [
    "Product engineering",
    "Full-stack engineering",
    "AI engineering",
    "AI agents",
    "Internal platforms",
    "Backend engineering",
    "Data systems",
    "Blockchain security",
  ],
});

const structuredData = [websiteSchema, personSchema, webPageSchema];
---

<Layout
  title={title}
  description={description}
  image={image}
  imageAlt={seoConfig.defaultOgImageAlt}
  canonical="/"
  type="website"
  structuredData={structuredData}
>
  <Home />
  <About />
  <ExperienceSection />
  <ProjectsSection />
  <AchievementsSection />
  <BlogSection />
  <Contact />
  <ScrollUp />
</Layout>
